pipeline {
    agent any
    stages {      
        stage('Build Docker image') {
            steps {
                sh 'chmod +x ./agileskills-ui-image.sh'
                sh './agileskills-ui-image.sh'
            }
        }

    }

 }
